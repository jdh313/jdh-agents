# Upstream divergences — writing-for-agents

_Upstream: `mattpocock/skills` · `skills/productivity/writing-for-agents` · ledger current as of `reviewed_sha: 321658273cb1`_

Intentional divergences from upstream. Reviewed via `skillsmith:upstream-review` (2026-08-29) — do not re-flag these as findings. Read by `upstream-review` only; never referenced from `SKILL.md`.

`SKILL.md` and `SKILL-MECHANICS.md` are adopted from upstream's current body — the broadened "any document an agent consumes" scope, model-invocable, four-section mechanics split. Only additive/adaptive changes below. No fabricated attributions, no dropped content.

| Kind | What | Why |
|------|------|-----|
| changed | Successor adoption: upstream renamed and rescoped `skills/productivity/writing-great-skills` to `skills/productivity/writing-for-agents` at commit `1fc6573e0e30`, which deleted the old path. This plugin's `writing-great-skills/` directory was retired (via `trash`) and replaced by `writing-for-agents/`, rebuilt from upstream's new body rather than diffed forward. | Following upstream's own restructure rather than maintaining a superseded path. Verified via `gh api .../commits?path=...` that `1fc6573e0e30` is the rename commit and the old path no longer exists at `321658273cb1`. |
| changed | `upstream:` provenance block in frontmatter re-pinned to `path: skills/productivity/writing-for-agents`, `reviewed_sha: 321658273cb1`, `status: reviewed`. | This marketplace's convention for every adapted skill; upstream carries no such block. |
| changed | Adopted upstream's flip to model-invocable: `disable-model-invocation: true` is dropped, matching upstream's current frontmatter. | Reversing the prior adaptation's deliberate user-invoked choice (recorded in the old `writing-great-skills` ledger). Other skills in this marketplace need to reach this reference directly — e.g. as shared reference for skill-authoring guidance — which requires a `description` other skills can invoke. |
| added | `references/GLOSSARY.md`, carrying `_Avoid_:` antonym lists on every entry. Upstream **deleted** its glossary file in the restructure — `writing-for-agents/` at `321658273cb1` ships only `SKILL.md` and `SKILL-MECHANICS.md`, no glossary. | Kept deliberately as local enrichment: the antonym lists are this marketplace's addition, predating upstream's restructure, and are the reason this glossary was kept at all rather than dropped alongside upstream's own. Retitled and retiered to track the new upstream terminology (`In-File Step`/`In-File Reference`/`Disclosed Reference`, `External Reference` demoted to a subtype of `Disclosed Reference`, `Granularity` retired as a head term with its load-bearing content folded into `Model-Invoked` and `Post-Completion Steps`); gained `Cache` and `Negation` entries to track concepts upstream's new `SKILL.md` body introduced. Verified against fetched upstream bytes at `321658273cb1`. |
| added | `references/ADDENDA.md` — this marketplace's own authoring mechanics (provenance block convention, `allowed-tools` vs agent `tools:` semantics, auto-discovery layout, commit format). No upstream counterpart. | Bridges the generic reference to jdh-agents's own authoring mechanics that upstream doesn't cover. Pointer wording fires only when authoring *in this repo*, so it costs nothing on other reads. |
| kept | `references/GLOSSARY.md`, `references/SKILL-MECHANICS.md`, and `references/ADDENDA.md` live in the skill's `references/` subdirectory, not the skill root. | The compiler publishes `SKILL.md` plus the recognized resource subdirs (`references/`, `scripts/`, `assets/`); a sibling `.md` at the skill root is not copied. Carried forward from the prior adaptation's fix for the same issue. |
