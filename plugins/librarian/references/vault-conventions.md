# Vault conventions (pointer)

The vault owns its conventions — this file deliberately holds none, because a
duplicated copy here is what previously went stale (it described the archived
`Waites/` model). Read the canonical sources at need:

- `~/Loose Ends/.claude/CLAUDE.md` — frontmatter shape, naming, wikilink style,
  templates, folder taxonomy
- `~/Loose Ends/.claude/rules/wiki.md` — knowledge-wiki page types and layering
- `~/Loose Ends/.claude/rules/catalog.md` — software-catalog stances and triggers

_Thinned to a pointer 2026-07-07 (was drifted: described archived Waites/ structure)._
