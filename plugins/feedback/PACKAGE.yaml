schema: agentforge.package/v1
id: feedback
defaults: {"name":"feedback","version":"0.3.0","description":"A two-sided loop for improving Claude Code plugins from test sessions. The 'session' skill (tester side) turns the current transcript into a copy-pasteable report grading the plugins exercised; the 'triage' skill (author side) aggregates many reports into a prioritized action plan, routing each finding to a concrete fix. Both share one report-format contract.","author":{"name":"Jacob Hoehler"},"license":"Apache-2.0","keywords":["feedback","testing","dogfooding","session-review","triage","plugins"]}
artifacts:
  - {type: skill, pattern: 'skills/*/SKILL.md'}
payloads:
  include:
    - {source: references/}
targets:
  claude: {}
